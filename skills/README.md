# Manifest-Provided Skills

This public repository does not ship organization-specific agent skills.

Deployable skills should live in an organization's manifest repository and be
installed through `flux skills install --manifest <name>` or `flux onboard`.
