# Acme Workspace Defaults

Use the Acme handbook mount for meetings, support notes, the fleet registry,
decisions, projects, policy, and people context. Mount product source only when a task needs source
code.
